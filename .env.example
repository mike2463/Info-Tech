# Copy to .env (or data/.env) and fill in your key. NEVER commit the real .env.
OPENAI_API_KEY=sk-...

# Optional overrides (defaults shown). Only gpt-5-mini / gpt-5-nano are allowed.
# AGENT_MODEL=gpt-5-nano
# EXTRACTION_MODEL=gpt-5-mini
# REASONING_EFFORT=low
# OUTPUT_DIR=./outputs
